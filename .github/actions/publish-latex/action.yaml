name: Semantic Release (LaTeX)
inputs:
  github-token-path:
    description: "The path of GitHub token"
    required: false
    default: "op://DevOps/Git PAT - StoneHome - Action/credential"
  onepass-token:
    description: "1password connect token"
    required: true

runs:
  using: composite
  steps:
    # 1. Setup Node (Required for Semantic Release tool only)
    - uses: actions/setup-node@v4
      with:
        node-version: 20

    # 2. Setup LaTeX (The Heavy Logic)
    - name: Cache TeX Live
      uses: actions/cache@v4
      with:
        key: ${{ runner.os }}-texlive-latest
        path: /usr/local/texlive

    - name: Install TeX Live
      shell: bash
      run: |
        sudo apt-get update
        sudo apt-get install -y texlive-full

    # 3. Auth
    - name: Setup 1Password
      uses: ./.github/actions/setup-1password@trunk
      with:
        onepass-token: ${{ secrets.onepass-token }}

    - name: Get Bot Token
      id: get-token
      uses: ./.github/actions/load-github-token@trunk
      with:
        secret-path: ${{ inputs.github-token-path }}

    # 4. Release (Semantic Release will likely run latex build plugins here)
    - name: Semantic Release
      shell: bash
      run: npx semantic-release@21.0.2
      env:
        GITHUB_TOKEN: ${{ steps.get-token.outputs.token }}
